[project]
name = "yolo-negative-flip"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mypy>=1.18.2",
    "pyyaml>=6.0.3",
    "ruff>=0.14.3",
    "tqdm>=4.67.1",
    "ultralytics>=8.3.223",
]

[tool.ruff]
exclude = ["*.ipynb"]
